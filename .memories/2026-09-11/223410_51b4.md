---
id: checkpoint_51b445df
timestamp: 2026-09-11T22:34:10.911Z
tags:
  - deployment-story
  - plugin
  - review-fixes
  - docs
git:
  branch: v8-deployment
  commit: 193cb03
  files:
    - CLAUDE.md
    - README.md
    - bin/archives/julie-v8.0.0-x86_64-unknown-linux-gnu.tar.gz
    - bin/install-opencode.cjs
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "Review fixes: installer block, README facts, STE"
---

## Review fixes: installer block, README facts, STE

- **WHAT:** `bin/install-opencode.cjs` no longer prints `environment.JULIE_WORKSPACE` in the `opencode.json` block; the note says OpenCode starts the server in the project directory and names the key for the exception. README: Node.js 22.5 prerequisite stated; OpenCode block matches the installer; the JULIE_WORKSPACE section says the launcher binds `JULIE_WORKSPACE`, else the start directory (no MCP roots claim); "one install path per harness". CLAUDE.md: "one install path per harness", the Codex and Antigravity sentence split into four STE sentences.
- **WHY:** An adversarial review of the deployment story docs found the printed block pins every OpenCode session to one project when pasted into the global config, a stale "client roots" claim, a missing Node prerequisite, and one 35-word sentence.
- **HOW:** Docs and one string in the installer. `node --test hooks/*.test.cjs` 21 pass. A `--help` probe ran the installer by accident; `--uninstall` removed the links again and `~/.config/opencode/skills/` is empty.
- **IMPACT:** Both READMEs now agree on the six install paths.
